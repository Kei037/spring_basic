<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_basic.mapper.MemberMapper">

    <insert id="insert">
        INSERT INTO member (memberId, memberPw, memberName, email, phoneNumber,
                            zipCode, address1, address2, addDate)
        VALUES (#{memberId}, password(#{memberPw}), #{memberName}, #{email}, #{phoneNumber},
                #{zipCode}, #{address1}, #{address2}, now())
    </insert>

</mapper>